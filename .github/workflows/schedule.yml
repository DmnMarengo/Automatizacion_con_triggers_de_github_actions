name: Scheduled Trigger

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  mensaje:
    runs-on: ubuntu-latest

    steps:
      - name: Mostrar mensaje
        run: echo "Ejecución programada"
